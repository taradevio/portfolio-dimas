import Typed from "typed.js";

      const autoType = new Typed(".auto-type", {
        strings: ["Dimas", "a Graphic Designer"],
        typeSpeed: 90,
        backSpeed: 90,
        loop: true,
      });